/** Public admin / site API base (no trailing slash). Used by the app for trusted mutations.
 * Prefer www — apex mysurro.com 307-redirects and can break non-GET methods.
 */
export const APP_API_BASE_URL = 'https://www.mysurro.com';
